export function Pagination({ page, totalPages, hasMore, onPageChange }: { page: number; totalPages?: number; hasMore?: boolean; onPageChange: (page: number) => void }) { const resolvedTotalPages = totalPages ?? (hasMore ? page + 1 : page); return <nav aria-label="Paginação" className="ui-pagination"><button disabled={page <= 1} onClick={() => onPageChange(page - 1)}>Anterior</button><span>Página {page} de {resolvedTotalPages}</span><button disabled={hasMore === undefined ? page >= resolvedTotalPages : !hasMore} onClick={() => onPageChange(page + 1)}>Próxima</button></nav>; }
