import { BookOpen, Building2, CalendarCheck, CircleHelp, ClipboardCheck, LayoutDashboard, Link2, Menu, Settings, Users, X } from 'lucide-react';
import { useState } from 'react';
import { NavLink } from 'react-router-dom';

const operational = [['Resumo da Semana', '/', LayoutDashboard], ['Meus Cursos', '/meus-cursos', BookOpen], ['Escolas', '/escolas', Building2], ['Alunos', '/alunos', Users], ['Pendências', '/pendencias', ClipboardCheck], ['Follow-up', '/followup', ClipboardCheck], ['Conexões Moodle', '/conexoes', Link2]] as const;
const management = [['Tarefas', '/tarefas', CalendarCheck], ['Agenda', '/agenda', CalendarCheck], ['Mensagens', '/mensagens', CalendarCheck], ['Relatórios', '/relatorios', CalendarCheck], ['Configurações', '/configuracoes', Settings], ['Suporte', '/suporte', CircleHelp]] as const;
function Links({ items, close }: { items: readonly (readonly [string, string, typeof LayoutDashboard])[]; close: () => void }) { return <>{items.map(([label, to, Icon]) => <NavLink key={to} to={to} end={to === '/'} onClick={close}><Icon size={17} />{label}</NavLink>)}</>; }
export function AppSidebar() { const [open, setOpen] = useState(false); const close = () => setOpen(false); return <><button className="mobile-menu" aria-label="Abrir menu" onClick={() => setOpen(true)}><Menu /></button><aside className={`sidebar ${open ? 'sidebar-open' : ''}`}><button className="mobile-close" aria-label="Fechar menu" onClick={close}><X /></button><div className="brand"><div className="brand-mark">M</div><div><strong>Moodle Connector</strong><span>Portal acadêmico</span></div></div><nav aria-label="Menu principal"><p>OPERACIONAL</p><Links items={operational} close={close} /><p>GESTÃO</p><Links items={management} close={close} /></nav><div className="sidebar-user"><span className="avatar">P</span><div><strong>Portal v2</strong><span>Fundação</span></div></div></aside></>; }
