import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { render, screen, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { DashboardPage } from '../features/dashboard/DashboardPage';
import { dashboardGateway } from '../features/dashboard/dashboard-gateway';
import { coursesGateway } from '../features/courses/courses-gateway';
vi.mock('../features/dashboard/dashboard-gateway', async () => ({ ...(await vi.importActual('../features/dashboard/dashboard-gateway')), dashboardGateway: { get: vi.fn() } }));
vi.mock('../features/courses/courses-gateway', async () => ({ ...(await vi.importActual('../features/courses/courses-gateway')), coursesGateway: { list: vi.fn() } }));
describe('DashboardPage', () => { beforeEach(() => vi.clearAllMocks()); it('loads indicators and the course selector through the current gateways', async () => { vi.mocked(dashboardGateway.get).mockResolvedValue({ data: { summary: { activeCourses: 2, pendingDeliveries: 3, awaitingGrading: 0, studentsAtRisk: 1, studentsNeedingAttention: 2 }, priorities: [], activitiesToReview: [], recentActivity: [], warnings: [] }, meta: { generatedAt: '2026-08-10T00:00:00Z' } }); vi.mocked(coursesGateway.list).mockResolvedValue({ data: [{ connectionRef: 'demo', courseId: '42', fullName: 'Curso de demonstração', shortName: 'Demo', visible: true }], meta: { page: 1, pageSize: 100, returned: 1, hasMore: false, generatedAt: '2026-08-10T00:00:00Z' } }); const client = new QueryClient({ defaultOptions: { queries: { retry: false } } }); render(<QueryClientProvider client={client}><MemoryRouter><DashboardPage /></MemoryRouter></QueryClientProvider>); await waitFor(() => expect(screen.getByText('Eventos hoje')).toBeInTheDocument()); expect(screen.getByText('Visão por Curso')).toBeInTheDocument(); expect(screen.getByText('Curso de demonstração')).toBeInTheDocument(); expect(dashboardGateway.get).toHaveBeenCalledTimes(1); expect(coursesGateway.list).toHaveBeenCalledTimes(1); }); });
