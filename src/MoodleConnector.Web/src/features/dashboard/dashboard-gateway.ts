import { createAppClient, type AppClient } from '../../integrations/http/api-client';
export type DashboardPriority = { key: string; title: string; detail: string; level: string; courseId?: string; studentId?: string };
export type DashboardActivity = { key: string; title: string; detail: string; occurredAt?: string; courseId?: string; studentId?: string };
export type DashboardResponse = { data: { summary: { activeCourses: number; pendingDeliveries: number; awaitingGrading: number; studentsAtRisk: number; studentsNeedingAttention: number; todayEvents?: number | null; todayTasks?: number | null; activitiesToReview?: number | null; activeNormalStudents?: number | null; pendingSubmissionAssignments?: number | null; pendingCorrectionAssignments?: number | null; newAtRiskThisWeek?: number | null; activeStudents?: number | null }; priorities: DashboardPriority[]; activitiesToReview: DashboardPriority[]; recentActivity: DashboardActivity[]; connectionRef?: string; warnings: string[]; week?: 'current' | 'last'; weekStartsAt?: string; weekEndsAt?: string }; meta: { generatedAt: string; connectionRef?: string } };
export const createDashboardGateway = (client: AppClient = createAppClient()) => ({ get: (connectionRef?: string, courseId?: string, activityOnly = false, week: 'current' | 'last' = 'current') => { const query = new URLSearchParams(); if (connectionRef) query.set('connectionRef', connectionRef); if (courseId) query.set('courseId', courseId); if (activityOnly) query.set('activityOnly', 'true'); if (week !== 'current') query.set('week', week); const suffix = query.toString() ? `?${query}` : ''; return client.get<DashboardResponse>(`/api/dashboard${suffix}`); } });
export const dashboardGateway = createDashboardGateway();


