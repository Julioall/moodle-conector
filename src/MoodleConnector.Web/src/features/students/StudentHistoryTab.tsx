import type { Student } from './students-gateway';
export function StudentHistoryTab({ student }: { student: Student }) { return <section aria-label="Histórico" className="student-history"><p>Primeiro acesso: {student.firstAccessAt ? new Date(student.firstAccessAt).toLocaleString('pt-BR') : 'Não informado'}</p><p>Último acesso: {student.lastAccessAt ? new Date(student.lastAccessAt).toLocaleString('pt-BR') : 'Nunca'}</p>{student.courses.map(course => <p key={`${course.connectionRef}:${course.courseId}`}>{course.name}: {course.lastCourseAccessAt ? new Date(course.lastCourseAccessAt).toLocaleString('pt-BR') : 'Sem acesso ao curso'}</p>)}</section>; }
