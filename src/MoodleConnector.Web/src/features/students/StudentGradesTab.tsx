import { Card, CardContent, CardHeader, CardTitle } from '../../components/ui/card';
import type { StudentCourse } from './students-gateway';
export function StudentGradesTab({ courses }: { courses: StudentCourse[] }) { return <section aria-label="Notas" className="student-tab-grid">{courses.length === 0 ? <p>Nenhuma nota encontrada.</p> : courses.map(course => <Card key={`${course.connectionRef}:${course.courseId}`}><CardHeader><CardTitle>{course.name}</CardTitle></CardHeader><CardContent>{course.grades.length === 0 ? <p>Sem notas registradas.</p> : course.grades.map(grade => <div className="student-grade-row" key={grade.itemId}><span>{grade.name}</span><strong>{grade.percentage != null ? `${grade.percentage}%` : grade.grade ?? 'Sem nota'}</strong></div>)}</CardContent></Card>)}</section>; }
