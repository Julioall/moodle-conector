import { useQuery } from '@tanstack/react-query';
import { Card, CardContent, CardHeader, CardTitle } from '../../components/ui/card';
import { reportsGateway } from './reports-gateway';
export function ReportsPage() { const query = useQuery({ queryKey: ['portal', 'reports', 'operational'], queryFn: reportsGateway.operational }); return <main className="content-frame"><header className="page-heading"><div><p className="eyebrow">GESTÃO</p><h1>Relatórios</h1><p>Indicadores operacionais determinísticos para acompanhamento manual.</p></div>{query.data && <span className="freshness">Atualizado em {new Date(query.data.meta.generatedAt).toLocaleString('pt-BR')}</span>}</header>{query.isPending && <p>Carregando relatório…</p>}{query.isError && <Card><CardContent><p role="alert">Não foi possível carregar o relatório.</p></CardContent></Card>}{query.data && <section className="dashboard-stats" aria-label="Relatório operacional"><Card><CardHeader><CardTitle>Tarefas abertas</CardTitle></CardHeader><CardContent><strong>{query.data.data.openTasks}</strong></CardContent></Card><Card><CardHeader><CardTitle>Tarefas concluídas</CardTitle></CardHeader><CardContent><strong>{query.data.data.completedTasks}</strong></CardContent></Card><Card><CardHeader><CardTitle>Eventos próximos</CardTitle></CardHeader><CardContent><strong>{query.data.data.upcomingEvents}</strong></CardContent></Card><Card><CardHeader><CardTitle>Follow-ups</CardTitle></CardHeader><CardContent><strong>{query.data.data.followupsRecorded}</strong></CardContent></Card></section>}</main>; }
