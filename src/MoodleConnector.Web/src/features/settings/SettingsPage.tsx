import { useQuery } from '@tanstack/react-query';
import { Card, CardContent, CardHeader, CardTitle } from '../../components/ui/card';
import { connectionsGateway } from '../connections/connections-gateway';
export function SettingsPage() { const query = useQuery({ queryKey: ['portal', 'settings', 'connections'], queryFn: connectionsGateway.list }); return <main className="content-frame"><header className="page-heading"><div><p className="eyebrow">CONFIGURAÇÕES</p><h1>Configurações</h1><p>Visibilidade operacional da sessão e das conexões Moodle.</p></div></header><Card><CardHeader><CardTitle>Conexões disponíveis</CardTitle></CardHeader><CardContent>{query.isPending && <p>Carregando…</p>}{query.isError && <p role="alert">Não foi possível carregar as conexões.</p>}{query.data?.data.map(connection => <article className="dashboard-row" key={connection.connectionRef}><div><strong>{connection.alias}</strong><span>{connection.host}</span></div><span>{connection.isDefault ? 'Padrão' : 'Disponível'} · {connection.status}</span></article>)}</CardContent></Card><Card><CardHeader><CardTitle>Segurança</CardTitle></CardHeader><CardContent><p>Credenciais Moodle não são expostas ao navegador. Operações mutáveis exigem CSRF e autorização no servidor.</p></CardContent></Card></main>; }
