import { useQuery } from '@tanstack/react-query';
import { Card, CardContent, CardHeader, CardTitle } from '../../components/ui/card';
import { connectionsGateway } from '../connections/connections-gateway';
export function SettingsPage() { const query = useQuery({ queryKey: ['app', 'settings', 'connections'], queryFn: connectionsGateway.list }); return <main className="content-frame"><header className="page-heading"><div><p className="eyebrow">CONFIGURAÃ‡Ã•ES</p><h1>ConfiguraÃ§Ãµes</h1><p>Visibilidade operacional da sessÃ£o e das conexÃµes Moodle.</p></div></header><Card><CardHeader><CardTitle>ConexÃµes disponÃ­veis</CardTitle></CardHeader><CardContent>{query.isPending && <p>Carregandoâ€¦</p>}{query.isError && <p role="alert">NÃ£o foi possÃ­vel carregar as conexÃµes.</p>}{query.data?.data.map(connection => <article className="dashboard-row" key={connection.connectionRef}><div><strong>{connection.alias}</strong><span>{connection.host}</span></div><span>{connection.isDefault ? 'PadrÃ£o' : 'DisponÃ­vel'} Â· {connection.status}</span></article>)}</CardContent></Card><Card><CardHeader><CardTitle>SeguranÃ§a</CardTitle></CardHeader><CardContent><p>Credenciais Moodle nÃ£o sÃ£o expostas ao navegador. OperaÃ§Ãµes mutÃ¡veis exigem CSRF e autorizaÃ§Ã£o no servidor.</p></CardContent></Card></main>; }

