interface:
  display_name: "Moodle Grading"
  short_description: "Generate grading corrections as CSV"
  default_prompt: "Use $moodle-grading to read submissions, generate grades and feedback, and export a CSV without changing Moodle."
